<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="180dp"
    android:orientation="vertical"
    android:background="#9B5CFF"
    android:padding="24dp"
    android:gravity="bottom">

    <!-- Close button (X) top right -->
    <ImageView
        android:id="@+id/btnCloseDrawer"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:src="@drawable/ic_close"
        android:layout_gravity="end"
        app:tint="#FFFFFF"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:focusable="true"
        xmlns:app="http://schemas.android.com/apk/res-auto"/>

    <!-- Drag handle (3 bars) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="8dp"
        android:gravity="center_horizontal">

        <View android:layout_width="28dp" android:layout_height="3dp" android:background="#FFFFFF" android:layout_marginBottom="4dp"/>
        <View android:layout_width="28dp" android:layout_height="3dp" android:background="#FFFFFF" android:layout_marginBottom="4dp"/>
        <View android:layout_width="28dp" android:layout_height="3dp" android:background="#FFFFFF"/>

    </LinearLayout>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Filters &amp; Views"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginTop="12dp"/>

</LinearLayout>